import Header from "@/components/shared/Header";
import HeroSection from "@/sections/HeroSection";

const Home = () => {
  return (
    <section>
      <Header />
      <HeroSection />
    </section>
  );
};

export default Home;
